# 课程设计汇报 · 组员任务安排

**项目**：基于飞桨的 CIFAR-10 图像分类识别系统
**汇报材料**：`image_classification_paddle/课程设计汇报.pptx`（14 页，每页备注栏就是该页讲稿）
**讲稿总量**：2973 字，约 14.9 分钟（按中文口播 200 字/分钟）；**加上现场演示约 1 分钟，总计约 15.9 分钟**
**团队**：5 人（A 为组长，即代码主要作者）
**每人讲稿**：`汇报讲稿/` 目录下 5 份，各自只含本人要讲的内容

---

## 一、总览：谁讲哪几页

| 成员                | 负责页码 | 主题                                                 | 讲稿字数 | 建议时长           |
| ------------------- | -------- | ---------------------------------------------------- | -------- | ------------------ |
| **A 靳毅航（组长）** | 1~4    | 开场 + 小组分工 + 项目背景 + 系统架构**兼现场演示** | 594 字 + 演示讲词 226 字 | 3.0 分 + 演示 1 分 |
| **B 李迎奥**         | 5、6、7  | 数据组织与预处理 + 模型搭建 + 模型训练               | 718 字   | 3.6 分钟           |
| **C 王锦**         | 8、9、10 | 评估结果 + 混淆矩阵 + 鲁棒性测试                     | 620 字   | 3.1 分钟           |
| **D 周弈恒**         | 11、12   | 推理性能 + 界面与容错                                | 418 字   | 2.1 分钟           |
| **E 铁承宇**         | 13、14   | 错误排查复盘 + 总结与展望                            | 623 字   | 3.1 分钟           |

合计 14 页 / 2973 字 / 讲稿 ≈14.9 分钟 + 现场演示 ≈1 分钟 = **总计约 15.9 分钟**。

**每人只上台一次、页码连续** —— 不必来回走台，交接次数从 6 次降到 4 次，现场更流畅。

> **时间不够时怎么砍**：优先压缩 B 的第 5 页（数据划分一句话带过）与第 6 页（残差块只讲一句）、D 的第 12 页（界面功能不逐条念）。
> **千万别砍**：C 的第 10 页（鲁棒性）和 E 的第 13 页（错误排查）—— 这两页是全场最能体现工程深度的部分。

---

## 二、逐人任务卡

### A（组长）

**负责页码**：第 1 页（封面/开场）、第 2 页（小组分工）、第 3 页（项目背景与目标）、第 4 页（系统设计）

**你要讲清的三件事**

1. 开场 30 秒内抛出三个数字：测试集准确率 91.75%、单张推理 3.23 毫秒、46 项自动化测试；
2. 为什么选 CIFAR-10（60000 张 32×32、10 类、分辨率低类间差异小）以及三个目标：精度达标、可交互、可交付；
3. 系统分四层，重点是"命令行与网页共用同一个推理函数、训练与评估共用同一套指标"，保证不同入口行为一致。

**上台前需掌握**（用于答问）

- `docs/technical_doc.md` 第 1 节（架构图）、第 2 节（接口表）、第 8 节（联调说明）
- 课程设计报告第 2 节（需求分析）

**可能被问到 & 回答方向**

- *为什么选 CIFAR-10？* → 60000 张 32×32 十类图像，分辨率低、类间差异小，既能把全流程跑通，又能在普通显卡上快速实验。
- *为什么这么分层？* → 单一职责：数据、模型、指标、IO 各自独立；且共用核心逻辑，避免不同入口结果不一致。
- *模块之间怎么联调？* → 命令行与网页共用同一个推理函数；评估与训练共用同一套指标与模型加载逻辑。

**兼任**：现场演示操作员（第 12 页由你亲自操作）

**演示职责**

- **汇报开始前**就把界面启动好并放在浏览器里备用（不要等讲到第 12 页才去开）；
- D 讲完第 12 页的界面功能介绍后，你接过鼠标，按文末演示脚本操作（约 60 秒）；
- **提前 1 小时**用演示电脑完整走一遍流程、确认模型文件正确、录好 30 秒备用视频。

**额外职责**：全场总控；遇到跨模块的硬技术问题由你兜底。

---

### B

**负责页码**：第 5 页（数据集组织与预处理）、第 6 页（模型选型与搭建）、第 7 页（模型训练）

**你要讲清的三件事**

1. 数据组织：45000 训练 / 5000 验证 / 10000 测试，用**固定随机种子**保证助教在自己机器上复现结果一致；训练用随机裁剪 + 水平翻转增强，验证/测试只做标准化；
2. 模型搭建：ResNet18 为 32×32 小图做的三处适配（首层卷积 3×3、去掉首个最大池化、末端保留全局平均池化）以及为什么——原版是给 224×224 设计的；共 1117 万参数；
3. 训练策略与曲线：第 20 轮学习率衰减后准确率台阶式跳升；第 30 轮后训练损失降但验证损失回升（过拟合信号）→ 早停在第 40 轮触发，最佳模型取自第 30 轮，验证准确率 92.56%。

**上台前需掌握**

- `docs/technical_doc.md` 第 3 节（模型结构对照）、第 4 节（训练流程）
- `data/dataset.py`、`data/preprocess.py`、`models/resnet.py`
- `outputs/results/train_curves.png`（曲线图会投在屏幕上，要能指着讲）

**可能被问到 & 回答方向**

- *为什么改首层卷积、去掉最大池化？* → 原版为 224×224 设计；32×32 若保留 7×7 步长 2 加池化，特征图到第二个阶段就只剩 1×1，网络无法继续。
- *数据增强做了什么、为什么？* → 随机裁剪（补 4 像素再裁回 32×32）+ 随机水平翻转，抑制过拟合、提升平移不变性。
- *为什么用固定种子？* → 保证数据划分和实验结果可复现，换台机器结果一致。
- *怎么判断过拟合的？* → 训练准确率 97% 但验证停在 92.5%，且第 30 轮后验证损失回升。

**额外职责**：汇报计时。约定暗号（如举杯/递纸条）提醒超时，每人超时 30 秒就提示。

---

### C

**负责页码**：第 8 页（评估结果）、第 9 页（混淆矩阵）、第 10 页（鲁棒性测试）

**你要讲清的三件事**

1. 测试集 10000 张（训练全程未接触）：准确率 91.75%、宏平均 F1 0.9174；汽车最高 0.959、猫最低 0.828；
2. 错误集中在语义相近类别：狗→猫 92 次、猫→狗 63 次、鸟→飞机 33 次，说明错误不是随机的；
3. **本页是全场重点**：五种扰动测试的对比 —— 裁剪平移只掉 0.2 个百分点（证明数据增强真的有效），噪声掉 55、低分辨率掉 69 个百分点（训练分布外，是明确短板）。

**上台前需掌握**

- `outputs/results/confusion_matrix.png`、`comparison_table.md`、`classification_report.txt`
- `data/preprocess.py` 里五种扰动函数的实现（`PERTURBATIONS`）

**可能被问到 & 回答方向**

- *为什么噪声下降这么多？* → 训练数据里没有噪声样本，属于分布外输入；且 32×32 图信噪比本来就低。改进方向是训练增强中加入高斯噪声。
- *为什么裁剪平移几乎不掉分？* → 训练时用了随机裁剪，平移不变性被真正"练"出来了 —— 这组数据正好量化了数据增强的收益。
- *测试集是怎么构造的？* → 取测试集前 2000 张，逐一施加扰动后走标准预处理；固定随机种子保证各项使用同一批样本，便于公平对比。

---

### D

**负责页码**：第 11 页（推理性能）、第 12 页（用户交互界面与容错性）

**你要讲清的三件事**

1. 单张 3.23 毫秒、权重 43 兆；batch 从 1 到 128 吞吐提升 16 倍 → 说明小批量 GPU 利用率不足，服务化部署应凑批推理；
2. 界面：上传（jpg/png/bmp/webp）→ Top-3 类别 + 置信度柱状图 + 可展开看全部 10 类概率；
3. 容错：模型缺失/图片损坏/格式不支持都给中文提示而非崩溃；各种图片模式与位深统一转 RGB。

**上台前需掌握**

- `docs/user_manual.md`（界面使用与常见问题）
- `app.py` 的界面逻辑；第 12 页讲稿
- 熟悉第 11、12 页的讲解节奏，并与 C 的第 10 页做好衔接

**可能被问到 & 回答方向**

- *界面怎么用的？* → 现场演示（见下方演示脚本）。
- *如果用户传了不属于这 10 类的图片？* → 系统内置了**开集识别**：最高置信度低于 50%、或最大 logit 低于 4.0 时，界面会用黄色警告把它判为**「其他」**，而不是硬给一个 10 类的答案（阈值标定见 `outputs/results/ood_calibration.json`）。局限是与 CIFAR-10 风格接近的平滑色块图仍可能被"自信地"误判，README 和说明书里都做了提示。
- *界面为什么快？* → 模型加载用资源缓存，整个会话只加载一次。

**第 12 页演示交接**

- 讲完界面功能介绍后说：「下面由组长为大家现场演示。」
- 然后**把鼠标（或操作位）交给组长，自己退到一旁**，不要再碰幻灯片；
- 等组长说「演示到此」并把操作位交给 E 之后，你再回到座位。

---

### E

**负责页码**：第 13 页（错误排查复盘）、第 14 页（总结与展望）

**你要讲清的三件事**

1. 两个真实故障的"现象 → 定位 → 修复"（详见下）；
2. 总结：全流程闭环 + 91.75% + 46 项测试 + 有分析深度（量化了增强收益、定位了短板）；
3. 展望：更强增强（Mixup/AutoAugment）或 ResNet34 提精度；训练中加入噪声与旋转补鲁棒性；扩展批量推理与 ONNX 导出。

**两个故障的讲法（照讲稿念即可，重点是"怎么定位的"）**

- **故障一**：命令行正常、网页上传任何图片都报 uint8 错误 → 拿到完整堆栈后定位到飞桨在静态图模式下拒绝 uint8 数组，而图像转换内部恰好把原始像素的 uint8 交给了飞桨 → 自己实现图像到张量的转换绕开该环节，并在入口自动切回动态图模式。
- **故障二**：上传第一张正常，之后任何图片都返回同一结果 → 推理函数被加了缓存，而图片参数命名以下划线开头，Streamlit 会忽略下划线参数参与缓存键计算，导致缓存键恒定不变 → 去掉缓存，并补了一个界面端到端测试（连续上传 3 张图断言结果各不相同）。

**上台前需掌握**

- `docs/technical_doc.md` 第 6 节（16 条错误排查记录）、第 7 节（优化记录）

**可能被问到 & 回答方向**

- *怎么保证这些问题不再出现？* → 两个 bug 都补了回归测试，测试总数从 31 项增加到 46 项，其中 `tests/test_app_ui.py` 用 Streamlit 官方测试框架真实执行界面并连续上传 3 张图片做断言。
- *项目还有什么不足？* → 坦白说三点：准确率距公开最好水平（93~94%）仍有差距；噪声与低分辨率鲁棒性是短板；未做模型量化与 TensorRT 加速。

**额外职责**：材料终检 —— 汇报前确认 PPT 能在演示电脑上正常打开、提交包解压后内容完整。

---

## 三、现场演示脚本（A 操作，约 60 秒）

演示安排在第 12 页。D 介绍完界面功能 → 说「下面由组长为大家现场演示」→ **D 把鼠标／操作位交给 A** → A 演示 → A 说「演示到此，下面请 E 做工程复盘与总结」→ **E 接过操作位**，自己翻到第 13 页开始。

**A 在汇报开始前**就要把界面启动好并停留在浏览器标签页（启动后别关，Streamlit 会话内模型只加载一次）：

```bash
cd image_classification_paddle
python -m streamlit run app.py
```

浏览器打开后：

1. 上传 `test_imgs/1_ship.png` → 应显示 **船 100.00%**（效果最直观）
2. 再上传 `test_imgs/20_horse.png` → 应显示 **马 88.92%**，顺带指出第 2 名是卡车 5.05%
3. 展开"查看全部 10 类概率分布"折叠区，说明界面支持看到完整分布
4. 最后上传 `test_imgs/ood_noise.png` → 界面会出现**黄色警告**并判为 **「其他」**，说明系统对不属于这 10 类的图片不会硬猜（开集识别）

> 若时间紧张，第 4 步可只讲一句带过，但**建议保留**——它是「完善用户交互界面 / 提升系统可靠性与容错性」这两项课程要求最直观的现场证据。

**备份方案**：若演示机打不开界面，改用命令行同样能出效果：

```bash
python predict.py --image test_imgs/1_ship.png
python predict.py --image test_imgs/ood_noise.png    # 开集识别，应输出「其他」
```

**再备份**：预先对上述操作录一段 30 秒屏幕录像，演示失败时直接播放。

---

## 四、答辩答问的协作规则

1. **谁讲谁答**：老师问到某页内容，由该页负责人回答，其他人不插话；
2. **组长兜底**：跨模块问题或涉及代码细节的问题，统一交给 A（他是代码主要作者）；
3. **不确定就承认**：回答不了就说"这一点我们没有深入验证，会后补充"，不要硬答；
4. **被指出错误时**：先接受，再说明我们的处理方式（例："老师指出的这个问题确实存在，我们的鲁棒性测试正好暴露了它，改进方向是…"）。

---

## 五、各人只需看的材料（省时间）

| 成员 | 必看                                                                     | 选看                          |
| ---- | ------------------------------------------------------------------------ | ----------------------------- |
| A    | 技术文档第 1、2、8 节；**演示脚本**                                | 课程设计报告第 2 节           |
| B    | 技术文档第 3、4 节；`data/` 两个文件；`models/resnet.py`；训练曲线图 | 课程设计报告第 4 节           |
| C    | `outputs/results/` 下的混淆矩阵、对比表与开集标定表；技术文档第 5 节 | 课程设计报告第 5.3、5.4 节    |
| D    | 用户使用说明书全文；`app.py`（含开集判定逻辑）；第 12 页讲稿       | 技术文档第 6 节的界面相关条目 |
| E    | 技术文档第 6、7 节                                                   | 课程设计报告第 6 节           |

讲稿就在 PPT 每一页的**备注栏**里（PowerPoint 用「幻灯片放映 → 使用演讲者视图」或 `Alt+F5` 查看），按上述页码分工照读即可。
